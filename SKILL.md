---
name: skill-view
description: 로컬에 설치된 Claude Code 스킬을 스캔해서 이름, 하는 일, 실행 커맨드, 설치·삭제 방법을 한국어 HTML 카탈로그로 만들어 보여준다. "무슨 스킬 깔려 있지", "스킬 목록 보여줘", "스킬 정리", "이 스킬 어떻게 지워", "what skills do I have" 같은 요청에 사용한다. 스킬을 새로 만들거나 편집할 때는 쓰지 않는다.
argument-hint: "[이름·설명에서 찾을 단어] [--source personal|project|plugin|builtin]"
---

# Skill View

설치된 스킬을 스캔해 **HTML 카탈로그**로 보여준다. 설치·삭제가 잦아도 매번 새로 훑으므로
항상 현재 상태를 반영한다.

이 스킬의 목적은 **"지금 뭐가 깔려 있는지 보기"**다. 정리·삭제는 사용자가 보고 나서 필요할 때
하는 후속 작업이지 이 스킬의 목표가 아니다. 그래서 삭제 명령을 앞세우지 않는다.

## 실행

스크립트는 **이 SKILL.md와 같은 디렉터리의 `scripts/scan-skills.mjs`**다. 실행 시 작업 디렉터리는
스킬 디렉터리가 아니라 사용자의 프로젝트이므로 상대 경로로 부르면 안 된다.

이 스킬이 로드될 때 함께 주어지는 **`Base directory for this skill:` 경로**를 쓴다.

```bash
node "<base directory>/scripts/scan-skills.mjs" --html "<스크래치패드>/skills.html" --bundled "<스크래치패드>/bundled.json" [검색어]
```

`--bundled`에 넣을 파일은 **실행 전에 직접 만든다.** 만드는 법은 아래 "Anthropic 기본 스킬" 참고.

그 값이 보이지 않으면 `~/.claude/skills/skill-view/scripts/scan-skills.mjs`를 쓰고,
없으면 `<프로젝트>/.claude/skills/skill-view/scripts/scan-skills.mjs`를 쓴다.
(`$CLAUDE_PLUGIN_ROOT`는 플러그인으로 설치했을 때만 설정되므로 의존하지 않는다.)

**작업 디렉터리는 바꾸지 않는다.** 스크립트가 현재 디렉터리를 프로젝트 루트로 보고
`.claude/skills`를 찾기 때문이다. 다른 프로젝트를 보려면 `cd` 대신 `--project <경로>`를 쓴다.

| 옵션 | 용도 |
|---|---|
| `--html <경로>` | **기본 형식.** HTML 카탈로그를 그 경로에 쓴다 |
| `--bundled <경로>` | **항상 같이 준다.** 기본 스킬 목록 + 모든 스킬의 사용 예시 JSON |
| `검색어` | 스킬 **이름·설명·예시**에 그 단어가 든 것만 남긴다. 부분 일치, 대소문자 무시, 한글 가능 |
| `--source personal\|project\|plugin\|builtin` | 출처 필터. 여러 번 지정 가능 |
| `--enabled-only` | 비활성 플러그인의 스킬 제외 |
| `--full` | (마크다운 전용) 설명을 200자에서 자르지 않음 |
| `--json` | 원본 데이터. description 전문과 `install`·`removal`·`examples` 필드 포함 |
| `--cache <경로>` | 번역 캐시 JSON이 있으면 설명을 한국어로 치환 |

`--html` 없이 부르면 마크다운 표가 나온다. 그건 다른 스킬이 파이프로 쓰거나 사용자가 명시적으로
"표로 줘"라고 할 때만 쓴다.

## 이 스킬을 부르는 예시

사용자가 뭐라고 하든 아래로 옮긴다. 인자를 안 줬으면 **전체**가 기본이다.

| 사용자가 이렇게 말하면 | 이렇게 부른다 | 결과 |
|---|---|---|
| "스킬 목록 보여줘", `/skill-view` | 인자 없이 | 전체를 출처별로 |
| "PDF 관련 스킬 뭐 있어" | `pdf` | 이름·설명·예시에 `pdf`가 든 것만 |
| "문서 만드는 스킬 찾아줘" | `문서` | `문서`가 든 것만 (한글 검색 됨) |
| "내가 깐 스킬만 보여줘" | `--source personal` | 개인 스킬만 |
| "기본 스킬 목록" | `--source builtin` | Anthropic 기본만 |
| "내 스킬이랑 이 프로젝트 스킬" | `--source personal --source project` | 두 출처 (여러 번 지정 가능) |
| "기본 스킬 중에 문서 관련" | `--source builtin 문서` | 검색어와 출처를 함께 |
| "왜 이 스킬이 안 뜨지" | `--enabled-only` 없이 전체 | 비활성 배지를 확인시킨다 |

검색어는 부분 일치다. 사용자가 말한 단어를 그대로 넘기면 대개 걸린다.
안 걸리면 더 짧게 자른다 (`스프레드시트 편집` → `스프레드시트`).

## 출력 규칙

1. **HTML을 만들고 Artifact로 게시한다.** 스크래치패드에 `skills.html`을 쓰고 `Artifact` 도구로
   그 파일을 게시한 뒤 링크를 준다. 터미널에 목록 전체를 다시 쏟아붓지 않는다 — 그게 이 스킬을
   만든 이유다. 페이지에 검색창과 출처 필터가 이미 들어 있다.
2. **터미널 답변은 서너 줄로 끝낸다.** 총 개수, 출처별 개수, 눈에 띄는 점(비활성 플러그인, 중복)
   정도만 적고 나머지는 링크에 맡긴다.
3. **카탈로그이지 정리 도구가 아니다.** "안 쓰는 건 지우세요" 같은 권유를 먼저 하지 않는다.
   설치·삭제 방법은 카드마다 이미 대등하게 적혀 있으므로, 사용자가 물어볼 때만 그 얘기를 꺼낸다.
4. **삭제는 절대 먼저 실행하지 않는다.** 사용자가 명시적으로 지우라고 해야 실행한다.
5. 플러그인 스킬은 **개별 설치·삭제가 불가능**하다. `claude plugin uninstall`은 그 플러그인의
   스킬 전부를 제거한다. 사용자가 플러그인 스킬 하나를 지우고 싶어 하면 이걸 먼저 말한다.
6. `비활성` 배지가 붙은 플러그인의 스킬은 지금 호출되지 않는다. 사용자가 "왜 이 스킬이 안 뜨지"라고
   물으면 여기부터 확인한다.
7. 중복 섹션이 페이지에 나타나면 한 줄 언급한다. 한쪽만 지워도 다른 쪽이 남아서 계속 보인다.
8. **Anthropic 기본 스킬은 항상 같이 넣는다.** `--bundled`로 목록을 주지 않으면 그 그룹이
   통째로 빠진다. 사용자가 "docx는 왜 없냐"고 묻게 되는 지점이므로 빠뜨리지 않는다.

## Anthropic 기본 스킬

`docx`, `code-review`, `design` 같은 기본 제공 스킬은 **디스크에 SKILL.md로 없다.** 세션이 직접
들고 있어서 스캔이 원리적으로 불가능하다. 그래서 목록을 스크립트에 넘겨야 한다.

`ListSkills` 도구는 계정에 붙은 일부만 돌려주므로 **쓰지 않는다.** 대신 이 세션 시스템 프롬프트의
**available skills 목록**을 그대로 옮겨 적는다. 그게 실제로 호출 가능한 전부다.

스캔 실행 전에 스크래치패드에 `bundled.json`을 쓴다:

```json
[
  {
    "name": "docx",
    "namespace": "anthropic-skills",
    "description": "Word 문서(.docx, .dotx)를 만들고 읽고 편집한다.",
    "examples": [
      { "say": "이 내용으로 Word 보고서 만들어줘", "does": "목차·서식이 잡힌 .docx 파일이 나옵니다" },
      { "say": "report.docx 읽고 요약해줘", "does": "문서의 글과 표를 뽑아 정리합니다" }
    ]
  }
]
```

- `namespace`는 커맨드에 접두사가 붙는 스킬만 넣는다 (`anthropic-skills:docx` → `/anthropic-skills:docx`).
  접두사가 없으면 생략하거나 빈 문자열.
- `description`은 **한국어로 한 줄 요약해서** 적는다. 이 스킬의 존재 이유가 한국어로 훑어보기다.
  영어 원문을 그대로 붙여넣지 않는다.
- 이 목록은 매 세션 새로 적는다. 하드코딩된 표를 스킬 안에 두지 않는 이유는 Anthropic이 스킬을
  추가·제거할 때마다 낡기 때문이다.

카드에 설치·삭제 줄은 붙지 않고 "기본 제공 — 따로 설치하거나 지울 수 없습니다"만 표시된다.

## 예시(`examples`)는 모든 스킬에 붙인다

설명만 읽어서는 "그래서 뭐라고 치라는 거냐"가 안 풀린다. 그래서 **카드마다** 실제 문장과 그 결과를
2~4개씩 넣는다. 이게 이 카탈로그의 핵심이다 — 빠뜨리면 그냥 이름 나열이 된다.

- `say`는 **사용자가 실제로 칠 법한 말**이다. 슬래시로 부르는 스킬은 커맨드 그대로
  (`/code-review high`), 파일·문서 스킬은 평소 말투로 (`"이 PDF에서 표만 뽑아줘"`).
  옵션이 있는 스킬은 대표 옵션을 하나씩 보여준다.
- `does`는 **그 결과 무슨 일이 일어나는지** 한 줄. 설명을 되풀이하지 말고 산출물을 적는다
  (`"목차·서식이 잡힌 .docx 파일이 나옵니다"`).
- **직접 부를 일이 없는 스킬**(`artifact-design`, `dataviz`, `workflow-authoring` 등)도 예시를 넣되,
  Claude가 알아서 읽는다는 걸 `does`에 밝힌다 — `"Claude가 먼저 이 가이드를 읽고 디자인을 잡습니다"`.
- **디스크에 설치된 스킬도 마찬가지다.** `bundled.json`에 같은 이름으로 넣으면 카드가 새로 생기지
  않고 예시만 얹힌다. 그 스킬의 SKILL.md에서 `argument-hint`와 본문을 읽고 예시를 뽑는다.
- 예시 문구도 페이지 검색 대상이다. `/skill-view 엑셀`처럼 설명에 없는 단어로도 찾히게 된다.

## 스캔 대상

| 출처 | 경로 | 설치 | 삭제 |
|---|---|---|---|
| 프로젝트 | `<프로젝트>/.claude/skills/<이름>/SKILL.md` | 그 아래에 폴더를 둔다 | 그 폴더를 지운다 |
| 개인 | `~/.claude/skills/<이름>/SKILL.md` | 그 아래에 폴더를 둔다 | 그 폴더를 지운다 |
| 플러그인 | `~/.claude/plugins/cache/<마켓>/<플러그인>/<버전>/skills/<이름>/SKILL.md` | `claude plugin install <플러그인>@<마켓>` | `claude plugin uninstall <플러그인>@<마켓>` |

각 SKILL.md의 frontmatter만 읽고 중단하므로 수백 개여도 1초 안에 끝난다. 네트워크 호출은 없다.

`~/.claude/plugins/marketplaces/`는 마켓플레이스 저장소 클론일 뿐 설치본이 아니라서 스캔하지 않는다.
실제 활성 스킬은 `cache/` 아래에 있다.

## 주의

- 스크립트는 스킬 디렉터리를 읽기만 한다. 쓰는 파일은 `--html`로 지정한 출력 하나뿐이고,
  아무것도 지우지 않는다.
- 커맨드 열은 frontmatter의 `name`과 `argument-hint`로 조합한 것이다. 스킬이 슬래시 커맨드를
  따로 등록해 두면 실제 호출명이 다를 수 있다.
